services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: umred_labo
      POSTGRES_USER: umred
      POSTGRES_PASSWORD: umred
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: